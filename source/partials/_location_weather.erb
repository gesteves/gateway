<%= config[:location] %>
<% if data.weather.present? && !current_page.data.hide_weather %>
  • <%= weather.currently.summary.downcase.humanize.gsub('\.$', '') %>, <%= "#{weather.currently.temperature.round}°F" %>
<% end %>
